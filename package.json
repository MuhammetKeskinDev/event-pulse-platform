{
  "name": "event-pulse-platform",
  "private": true,
  "scripts": {
    "test": "vitest run tests/unit",
    "test:coverage": "vitest run tests/unit --coverage",
    "test:integration": "cross-env RUN_INTEGRATION=1 vitest run tests/integration",
    "test:all": "vitest run",
    "verify:full": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/full-verify.ps1",
    "load-gen": "tsx tests/load/load-gen.ts",
    "seed-db": "tsx scripts/seed-db.ts",
    "trigger-rule-alert": "tsx scripts/trigger-rule-alert.ts"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "@vitest/coverage-v8": "^3.2.4",
    "cross-env": "^7.0.3",
    "pg": "^8.20.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "zod": "^4.3.6"
  }
}
