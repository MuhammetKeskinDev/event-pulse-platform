# EventPulse API — PDF: containerized services
FROM node:22-alpine
WORKDIR /app
COPY src/package.json src/package-lock.json ./
RUN npm ci
COPY src/ ./
RUN npm run build
EXPOSE 3000
CMD ["node", "dist/app.js"]
